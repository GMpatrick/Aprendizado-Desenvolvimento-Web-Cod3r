//armazenando uma função em uma variavel

const imprimirSoma = function (a, b){
    console.log(a + b)
}

imprimirSoma(2, 3)

// Armazendando uma função arrow em uma variavel
const soma = (a, b) => {
    return a + b
}

// Recurso da arrow function: Retorno implicito

const subtracao = (a , b) => a - b


